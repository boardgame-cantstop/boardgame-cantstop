version: '3.7'

services:
  app:
    image: node:latest
    volumes:
    - .:/ws
    entrypoint: /bin/sh -c 'tail -f'
    working_dir: /ws
    ports:
    - 80:80
    environment:
      PORT: 80
      HOST: 0.0.0.0
